#include <iostream>

/* Queue ADT (Abstract Data Type)
 * Operation:
 * enqueue(value) (push(value)) add value to the back <----
 * dequeue() (pop) remove value in the front              | constant time O(1)
 * front() (peek) to see the value in the front           | 
 * isempty() to check if queue is empty               <----
 */

 /* Application
  * Printer queue
  * Process Scheduling
  * Simulating wait
  */
